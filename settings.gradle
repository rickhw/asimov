rootProject.name = 'asimov'

include 'api-server'     // depends on core, and common

include 'event-consumer'    // depends on core, and common

include 'core'      // common libraries and domain core which only data model, w/o dependencies



